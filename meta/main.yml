---
galaxy_info:
  author: Ian Copp
  description: Install Homebrew Cask via Homebrew.
  license: MIT
  min_ansible_version: 1.2
  platforms:
    - name: MacOSX
      versions:
        - all
  galaxy_tags:
    - packaging

dependencies:
  - role: icopp.homebrew
    when: ansible_os_family == "Darwin"
